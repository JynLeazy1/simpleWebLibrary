pipeline {
    agent any
	tools {
		maven "maven3914"
	}
    stages {
        stage('Test') {
            steps {
               echo 'Testing'
	       	   //sh 'mvn package'
	    }
        }
		stage('Linting') {
		    steps {
				sh '''
				echo 'Linting'
				'''
				//sh 'mvn checkstyle:checkstyle'
		    }
		}	
		stage('Build') {
    	    steps {
		        echo 'Building'
				sh 'mvn -B -DskipTests clean package'
    	    }
    	}
		stage('Scanning') {
		    steps {
				echo 'Scanning'
		    }
		}
		stage('Push') {
		    steps {
				echo 'Pushing'
		    }
		}
    }
}
