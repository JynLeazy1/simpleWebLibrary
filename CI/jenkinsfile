pipeline {
    agent any

	tools {
		maven 'Maven-3.9.12' 
		jdk 'OpenJDK-21.0.9'
	}
    stages {
        stage('Test') {
            steps {
               echo 'Testing'
	       	   //sh 'mvn package'
	    }
        }
		stage('Linting') {
		    steps {
				echo 'Linting'
				//sh 'mvn checkstyle:checkstyle'
		    }
		}	
		stage('Build') {
    	    steps {
		        echo 'Building'
				//sh 'mvn -B -DskipTests clean install'
    	    }
    	}
		stage('Scanning') {
		    steps {
				echo 'Scanning'
		    }
		}
		stage('Push') {
		    steps {
				echo 'Pushing'
		    }
		}
    }
}
